rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 每个用户的云端状态文档：只允许读写自己的（uid 必须等于登录用户）
    match /states/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
